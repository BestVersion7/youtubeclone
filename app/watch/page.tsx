export default function WatchPage(props: { searchParams: { v: string } }) {
    const videoId = props.searchParams.v;

    return (
        <main>
            <div></div>
            <aside></aside>
        </main>
    );
}
